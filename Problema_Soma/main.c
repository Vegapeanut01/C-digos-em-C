#include <stdio.h>
#include <stdlib.h>

int main()
{
    int x, y, soma;

    printf("Digite o valor de x: ");
    scanf("%d", &x);
    printf("Digite o valor de y: ");
    scanf("%d",&y);

    soma = x + y;

    printf("SOMA = %d", soma);


    return 0;
}
